<Window
    x:Class="ConduitDEVAPP.Views.rootWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ConduitDEVAPP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">




    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="AppBar"
                   IsTextSelectionEnabled="False"
                   Text="Conduit"
                   Height="25"
                   >
            
        </TextBlock>
        <Grid Background="Gray" ColumnDefinitions="*, *" RowDefinitions ="*, *" Grid.Row="1">
            <Frame x:Name="topleftFrame" Grid.Column="0" Grid.Row="0" />
            <Frame x:Name="bottomleftFrame" Grid.Column="0" Grid.Row="1" />
            <Frame x:Name="toprightFrame" Grid.Column="1" Grid.Row="0" />
            <Frame x:Name="bottomrightFrame" Grid.Row="1" Grid.Column="1" />
        </Grid>
    </Grid>
</Window>
